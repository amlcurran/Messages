apply plugin: 'java'


dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])

    sourceSets {
        main {
            java {
                srcDir 'src'
            }
        }
    }
}